Algoritmo ejercicio2
	//Realizar un programa que comprueba si una cadena leída por teclado comienza por una subcadena
	// por teclado.
	Escribir "Introduce la cadena"
	Leer cad
	Escribir "Introduce la subcadena"
	Leer subca 
	
	longitudsubca<-Longitud(subca)
	longitudcad<-Longitud(cad)

	Si Subcadena(cad,0,Longitud(subca)-1)=subca Entonces
		Escribir "La cadena comienza por la subcadena"
	SiNo
		Escribir "La cadena NO comienza por la subcadena"
	FinSi
	
FinAlgoritmo
